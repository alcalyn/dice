version: '2'

services:
    python:
        build: docker/rpi
        container_name: dice_python
        volumes:
            - .:/var/project/dice
        working_dir: /var/project/dice
        command: sleep 10000000
